name: pino test
on: [push]

jobs:
  check-files:
    runs-on: ubuntu-latest
    steps:
      - name: code download
        uses: actions/checkout@v4

      - name: show files
        run: ls -r
        
      - name: Leer mensaje.txt
        run: cat mensaje.txt

      - name: Ejecutar mi script bash
        run: bash script.sh

